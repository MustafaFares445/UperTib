includes:
    - vendor/larastan/larastan/extension.neon
    - phpstan-baseline.neon

parameters:
    level: max

    paths:
        - app
        - bootstrap/app.php
        - config
        - database
        - routes

    excludePaths:
        - app/**/*.blade.php

    tmpDir: storage/framework/phpstan
